name: libero_spatial_tinyplus

task_name: libero_spatial_tinyplus

shape_meta: &shape_meta
  action:
    shape: [7]


dataset:
  _target_: oat.dataset.zarr_dataset.ZarrDataset
  zarr_path: data/libero/libero_spatial_tinyplus.zarr
  obs_keys: []
  action_key: action
  n_obs_steps: 0
  n_action_steps: ${horizon}
  seed: ${seed}
  val_ratio: 0.2
